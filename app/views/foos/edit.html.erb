<h1>Editing foo</h1>

<% form_for(@foo) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @foo %> |
<%= link_to 'Back', foos_path %>